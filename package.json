{
  "name": "cleep-mcp",
  "version": "1.0.1",
  "description": "MCP server for Cleep - expose captured ideas via Claude Code",
  "type": "module",
  "main": "build/server.js",
  "bin": {
    "cleep-mcp": "build/cli.js"
  },
  "files": [
    "build/"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
